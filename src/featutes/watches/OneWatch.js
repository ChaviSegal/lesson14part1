const OneWatch = ({one}) => {
    return ( <div>
        <h1>{one.name}</h1>
        <h1>{one.price}</h1>
        <h1>{one.album}</h1>
        <h1>{one.src}</h1>
    </div> );
}
 
export default OneWatch;